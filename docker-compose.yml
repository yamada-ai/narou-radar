services:
  app:
    image: python:3.11-slim
    working_dir: /work
    volumes:
      - ./:/work
    command: bash -lc "pip install -e . && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
